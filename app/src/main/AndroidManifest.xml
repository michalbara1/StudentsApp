<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.assigment2">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Assigment2"
        tools:targetApi="31">
        <activity
            android:name=".StudentDetailsActivity"
            android:exported="false" />
        <!-- Main entry point activity -->
        <activity android:name=".MainActivity" /> <!-- Activity that displays students and lets you edit them -->
        <activity
            android:name=".StudentsRecyclerViewActivity"
            android:exported="true" /> <!-- Activity to add new students -->
        <activity
            android:name=".AddStudentActivity"
            android:exported="false" /> <!-- Activity to edit existing student details -->
        <activity
            android:name=".EditStudentActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>